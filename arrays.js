"use strict";

let students_marks = [13,56,7,34,56];
console.log(`The total number of students is: ${students_marks.length}\n `); //.lenth prints the total number of elements in that array
console.log("The marks of the students are as follows\n");
for(let i = 0; i<students_marks.length; i++){
    console.log(students_marks[i]);
}